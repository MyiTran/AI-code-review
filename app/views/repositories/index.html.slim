.d-flex.flex-column.flex-lg-row.align-items-lg-center.justify-content-between.gap-3.mb-4
  div
    h1.page-title Repositories

    p.page-description
      | Connect repositories and configure automatic AI code review.

  = link_to connect_repositories_path,
      class: 'btn btn-primary',
      data: { turbo: false } do
    i.bi.bi-plus-lg.me-2
    | Connect repository

.dashboard-card.mb-4
  .p-4
    = form_with url: repositories_path,
        method: :get,
        local: true,
        class: 'row g-3 align-items-end' do |form|
      .col-12.col-xl-4
        = form.label :query,
            'Search repositories',
            class: 'form-label small fw-semibold'

        .input-group
          span.input-group-text.bg-white.border-end-0
            i.bi.bi-search.text-secondary

          = form.search_field :query,
              value: params[:query],
              placeholder: 'Search by repository name...',
              class: 'form-control border-start-0 ps-0'

      .col-6.col-md-4.col-xl-2
        = form.label :connection_status,
            'Connection',
            class: 'form-label small fw-semibold'

        = form.select :connection_status,
            options_for_select([['Connected', 'connected'],['Disconnected', 'disconnected']], params[:connection_status]),
            { include_blank: 'All connections' },
            class: 'form-select'

      .col-6.col-md-4.col-xl-2
        = form.label :language, 'Language', class: 'form-label small fw-semibold'

        = form.select :language,
            options_for_select(Array(@languages), params[:language]),
            { include_blank: 'All languages' },
            class: 'form-select'

      .col-6.col-md-4.col-xl-2
        = form.label :ai_model,
            'AI model',
            class: 'form-label small fw-semibold'

        = form.select :ai_model,
            options_for_select(Array(@models), params[:ai_model]),
            { include_blank: 'All models' },
            class: 'form-select'

      .col-6.col-md-4.col-xl-2
        .d-flex.gap-2
          = form.submit 'Apply',
              class: 'btn btn-primary flex-grow-1'

          = link_to repositories_path,
              class: 'btn btn-light',
              title: 'Clear filters' do
            i.bi.bi-arrow-counterclockwise

.dashboard-card
  .d-flex.flex-column.flex-md-row.align-items-md-center.justify-content-between.gap-2.p-4.border-bottom
    div
      h2.card-title-custom Connected repositories

      p.mb-0.mt-1.text-secondary.small
        | #{@repositories.count} repositories found

    .d-flex.align-items-center.gap-2.text-secondary.small
      i.bi.bi-shield-check
      | GitHub App connected

  - if @repositories.any?
    .table-responsive
      table.table.mb-0.repository-table
        thead
          tr
            th Repository
            th Language
            th AI model
            th AI auto review
            th Connection
            th Last reviewed
            th

        tbody
          - @repositories.each do |repository|
            tr
              td
                = link_to repository_path(repository),
                    class: 'repository-name-link' do
                  .d-flex.align-items-center.gap-3
                    span.repository-icon
                      i.bi.bi-github

                    div
                      p.mb-1.fw-semibold.text-dark
                        = repository.name

                      p.mb-0.text-secondary.small.repository-description
                        = repository.description.presence || 'No description'

              td
                span.language-badge
                  span.language-dot
                  = repository.language.presence || 'Unknown'

              td
                span.text-secondary.small
                  | Not selected

              td
                .d-flex.align-items-center.gap-2
                  .form-check.form-switch.mb-0
                    input.form-check-input.ai-review-toggle type="checkbox" role="switch" disabled=true aria-label="Toggle AI review for #{repository.name}"

                  span.small.fw-semibold.text-secondary
                    | Off

              td
                - if repository.connected?
                  span.badge.badge-soft-success.rounded-pill.px-3.py-2
                    i.bi.bi-link-45deg.me-1
                    | Connected
                - else
                  span.badge.bg-light.text-secondary.rounded-pill.px-3.py-2
                    i.bi.bi-link-45deg.me-1
                    | Disconnected

              td.text-secondary.small
                | Never

              td.text-end
                = link_to repository_path(repository),
                    class: 'btn btn-sm btn-light',
                    aria: { label: "View #{repository.name}" } do
                  i.bi.bi-chevron-right

  - else
    .text-center.p-5
      span.empty-state-icon.mb-3
        i.bi.bi-search

      h3.fs-5 No repositories found

      p.text-secondary.mb-4
        | Connect a GitHub repository to get started.

      = link_to connect_repositories_path,
          class: 'btn btn-primary',
          data: { turbo: false } do
        i.bi.bi-plus-lg.me-2
        | Connect repository
        